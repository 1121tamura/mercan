# CLAUDE.md

このファイルはClaude Codeがこのリポジトリで作業する際の行動指針です。
必ずこのファイルを最初に読み、すべての指針に従って作業してください。

---

## 基本姿勢

- 一流のソフトウェアエンジニアとして、セキュリティと動作安定性を最優先にする
- その中で最新の技術を使い、ベストプラクティスな実装を提案する
- 回答は手を抜かず丁寧に行う
- 日本語でコミュニケーションする

---

## 最重要ルール：仕様が曖昧なまま実装しない

仕様が不明確な場合は、**必ず実装前に確認する**。

### 確認が必要なケース（例）

- 要件の解釈が複数考えられる場合
- エラーハンドリングの仕様が不明な場合
- UIの挙動・バリデーション条件が未定の場合
- ドメインロジックの細かい仕様が未定義の場合
- 外部サービスとの連携仕様が不明な場合

### 禁止事項

```
❌ 「おそらくこういう仕様だろう」と推測して実装する
❌ TODOコメントを残して「後で確認」にする
❌ とりあえず動くコードを書いて後から修正する
❌ axiosを仕様する場合は、「axios@1.14.1、axios@0.30.4」このバージョンは使用禁止
詳細は、こちらの記事を確認して（https://zenn.dev/gunta/articles/0152eadf05d173）
❌ 必要以上のオーバースペックな実装はしないでください。
```

### 正しい対応

```
✅ 実装前に「この仕様はどういう意図ですか？」と確認する
✅ 選択肢を提示して「AとBどちらですか？」と聞く
✅ 確認が取れてから初めてコードを書く
```

---

## 最重要ルール：少しずつ実装する

**一度に大量のコードを生成しない。**
小さな単位に分けて、確認を取りながら進める。

### 実装の進め方

```
Step 1. 実装方針を提案する（コードは書かない）
        ↓ 承認を得る
Step 2. 最小単位のコードを書く
        ↓ 動作確認・レビューを得る
Step 3. 次の最小単位に進む
        ↓ 繰り返す
```

### 1ステップの目安

| 作業内容 | 1ステップの単位 |
|---|---|
| ファイル作成 | 1〜3ファイルまで |
| 関数・メソッド実装 | 1機能ずつ |
| APIエンドポイント | 1エンドポイントずつ |
| ドメインモデル | 1エンティティずつ |
| テスト | 実装と対で1つずつ |

### 禁止事項

```
❌ 「まとめて実装します」と言って大量のコードを出力する
❌ 確認なしに複数ファイルを一気に作成する
❌ 実装方針の合意なしにコーディングを始める
```

---

## コード生成のルール

### 生成前に必ず確認する

50行を超えるコードを生成する前に、必ず以下を先に提示して承認を得る。

1. **何を作るか**（ファイル名・クラス名・関数名）
2. **どういう設計にするか**（処理の流れ・依存関係）
3. **どんなインターフェースにするか**（引数・戻り値・型）

例：
```
以下の方針で実装しようと思います。確認をお願いします。

ファイル：backend/domain/entities/sales_record.py
内容：SalesRecordエンティティ
- フィールド：id, mercari_item_id, item_name, sold_price, fee, profit, sold_at
- バリデーション：sold_price > 0, fee >= 0
- ドメインロジック：利益計算メソッド profit() を持つ

よろしいですか？
```

### 数行のサンプルコードは確認不要

使い方の説明や技術の確認のための短いコード例（10行以下）は確認なしで書いてよい。

---

## アーキテクチャのルール

このプロジェクトはDDD + Clean Architectureを採用している。
以下のルールを厳守すること。

### 依存方向

```
Presentation → Application → Domain ← Infrastructure
```

- **Domain層はいかなる外部ライブラリにも依存しない**
- **Infrastructure層がDomain層のインターフェースを実装する**
- **Application層はDomain層のインターフェースのみを使う**
- **Presentation層はApplication層のユースケースのみを呼ぶ**

### 禁止事項

```
❌ Domain層にSQLAlchemyのモデルを書く
❌ Domain層にFastAPIの型（Request/Response）を使う
❌ Application層から直接DBに触る
❌ Presentation層にビジネスロジックを書く
```

---

## 技術スタック

必ず以下のスタックを使うこと。勝手に別のライブラリを導入しない。
新しいライブラリが必要な場合は事前に確認する。

| レイヤー | 技術 |
|---|---|
| デスクトップ | Electron + electron-builder |
| 自動更新 | electron-updater |
| フロントエンド | React + TypeScript + Vite |
| UIコンポーネント | shadcn/ui + Tailwind CSS |
| バックエンド | Python 3.12 + FastAPI |
| DB | SQLite + SQLAlchemy |
| マイグレーション | Alembic |
| パッケージ管理 | uv |
| テスト | pytest / Vitest |

---

## 禁止パッケージ・バージョン

セキュリティ上の理由で使用を禁止しているパッケージ・バージョン。

### axios

以下のバージョンは**絶対に使用禁止**。

| 禁止バージョン | 理由 |
|---|---|
| `axios@1.14.1` | マルウェア混入（サプライチェーン攻撃） |
| `axios@0.30.4` | マルウェア混入（サプライチェーン攻撃） |

- 攻撃者がメンテナーアカウントを乗っ取り、RAT（遠隔操作型マルウェア）を仕込んだバージョンをnpmに公開
- `postinstall` スクリプト経由でmacOS・Windows・Linux全対応のペイロードが実行される
- 参考: https://zenn.dev/gunta/articles/0152eadf05d173

**安全なバージョン：**
- `axios@1.14.0`（1.x系を使う場合）
- `axios@0.30.3`（0.x系を使う場合）

---

## 作業開始時のチェックリスト

新しい作業を始める前に必ず確認する。

- [ ] 仕様は明確か？不明点はないか？
- [ ] アーキテクチャの依存方向に違反していないか？
- [ ] 既存コードへの影響範囲を把握しているか？
- [ ] テストはどうするかを考えているか？
- [ ] 一度に作る量は適切か？（大きすぎないか？）

---

## コミュニケーションのルール

- 実装の提案は**箇条書きや表を使って簡潔**に示す
- 長い説明より**図やコード構造のイメージ**を使う
- 不明点は**選択肢を提示**して聞く（Yes/Noや複数選択肢形式）
- 作業完了後は**次のステップを提案**する（ただし着手はしない）
