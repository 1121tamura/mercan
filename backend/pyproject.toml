[project]
name = "mercan-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "gunicorn",
    "sqlalchemy",
    "alembic",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]
